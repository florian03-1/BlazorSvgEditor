@using BlazorSvgEditor.SvgEditor.Helper
@inherits ShapeEditor<Circle>

<circle @ref=ElementReference

        @onpointerdown="Select"

        @onpointerenter="Enter"
        @onpointerleave="Leave"

        cx=@SvgElement.Cx.ToInvariantString()
        cy=@SvgElement.Cy.ToInvariantString()
        r=@SvgElement.R.ToInvariantString()

        stroke="@SvgElement.Stroke"
        stroke-width="@SvgElement.StrokeWidth"
        stroke-linecap="@SvgElement.StrokeLinecap"
        stroke-linejoin="@SvgElement.StrokeLinejoin"
        stroke-dasharray="@SvgElement.StrokeDasharray"
        stroke-dashoffset="@SvgElement.StrokeDashoffset.ToInvariantString()"

        fill="@SvgElement.Fill"
        fill-opacity="@SvgElement.FillOpacity.ToInvariantString()"

        style="cursor: @(SvgElement.State == ShapeState.Selected ? "move" : "pointer")">
</circle>

@if (SvgElement.State == ShapeState.Selected && SvgElement.SvgEditor.EditMode != EditMode.Add && SvgElement.SvgEditor.EditMode != EditMode.Move)
{
        <Anchor OnPointerDown="() => OnAnchorSelected(0)" CssCursor="ew-resize" Position="new Coord<double>(SvgElement.Cx + SvgElement.R, SvgElement.Cy)" />
        <Anchor OnPointerDown="() => OnAnchorSelected(1)" CssCursor="ew-resize" Position="new Coord<double>(SvgElement.Cx - SvgElement.R, SvgElement.Cy)" />
        <Anchor OnPointerDown="() => OnAnchorSelected(2)" CssCursor="ns-resize" Position="new Coord<double>(SvgElement.Cx, SvgElement.Cy + SvgElement.R)" />
        <Anchor OnPointerDown="() => OnAnchorSelected(3)" CssCursor="ns-resize" Position="new Coord<double>(SvgElement.Cx, SvgElement.Cy - SvgElement.R)" />
}